{
  "name": "aws-lambda",
  "version": "0.1.0",
  "bin": {
    "aws-lambda": "bin/aws-lambda.js"
  },
  "scripts": {
    "clean": "rimraf lib/**/.d.ts lib/**/*.js ",
    "rebuild": "npm run clean && npm run build",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.172.0",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.172.0",
    "constructs": "^10.0.0"
  }
}
